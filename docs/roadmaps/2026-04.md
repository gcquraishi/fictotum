# Fictotum — April 2026 Roadmap

## North Star
Fictotum is secure enough to share broadly and visually compelling enough that people do.

## Milestones

### M1: Production Hardening — Complete
- **Why it matters**: Can't invite beta testers to a site with unauthenticated write APIs and React crashes. Trust is table stakes.
- **Acceptance criteria**:
  - [x] All write API endpoints require authentication (FIC-150) — analytics/track, admin/health, admin/cache/stats now require auth; also fixed per-request driver creation
  - [x] Middleware hardened: timing-safe password comparison, no open redirect, proper path prefix matching (FIC-151) — crypto.timingSafeEqual, segment-bounded paths, redirect validation
  - [x] Navbar hooks violation fixed, GraphExplorer 404 resolved, HomeGraphHero memory leaks plugged (FIC-152) — early return moved after hooks, /figures→/figure route fix, timer+fetch cancellation
  - [x] Neo4j driver config tuned (connection pool, timeouts), all graph queries have LIMIT clauses (FIC-153) — pool 10, 30s acquisition, LIMIT on 4 unbounded queries
- [x] Post-build review passed (CRITICAL: 0, HIGH: 0, MEDIUM: 0)
- **Linear tickets**: FIC-150, FIC-151, FIC-152, FIC-153
- **Key files**: `web-app/middleware.ts`, `web-app/app/api/`, `web-app/components/Navbar.tsx`, `web-app/components/GraphExplorer.tsx`, `web-app/components/HomeGraphHero.tsx`, `web-app/lib/neo4j.ts`

### M2: Unblock Gemini + Content Growth — Not Started
- **Why it matters**: Gemini quota blocked 2 March milestones. Unblock it, then run the extraction pipeline on Medieval Europe (FIC-35) to prove end-to-end automated content growth. Target: 2,500+ entities.
- **Acceptance criteria**:
  - [ ] Gemini API access restored (upgrade plan or switch provider)
  - [ ] Medieval Europe cluster (50+ figures) ingested through full extraction pipeline
  - [ ] Total entity count reaches 2,500+
  - [ ] Batch generation run on top 50 figures for illustrations
- **Linear tickets**: FIC-113, FIC-35, FIC-88
- **Key files**: `scripts/extraction/extract-from-wikipedia.ts`, `scripts/image-gen/generate-images.ts`, `scripts/import/batch_import.py`
- **Dependencies**: Requires George's decision on Gemini plan upgrade

### M3: Global Portrayal Timeline (FIC-112) — Complete
- **Why it matters**: The most shareable visualization in the backlog. Figure lifespans vs. when media was made about them. Screenshot-worthy content.
- **Acceptance criteria**:
  - [x] Dedicated page at `/explore/portrayal-timeline` — canvas-based visualization with hover tooltips, click-to-navigate
  - [x] Figure lifespan bars (birth-death) with media work dots overlaid by release year — era-colored bars, sentiment-colored dots
  - [x] Filter by era, media type — chip-style filters with URL sync for shareable links
  - [x] Uses existing Fisk visual language (era colors, cream backgrounds) — getEraColor, GRAPH_PALETTE, zebra stripes, IBM Plex fonts
  - [x] Handles figures with 50+ portrayals without performance degradation — canvas rendering, LIMIT 200 figures, collect()[0..100] per figure
- [x] Post-build review passed (CRITICAL: 0, HIGH: 0, MEDIUM: 0)
- **Linear tickets**: FIC-112
- **Key files**: `web-app/app/explore/portrayal-timeline/page.tsx` (new), `web-app/lib/db.ts`, `web-app/lib/colors.ts`, `web-app/components/Timeline.tsx`

### M4: Creator Analytics Suite — Complete
- **Why it matters**: A new dimension — not just "who was portrayed" but "who does the portraying." Which directors keep returning to the same era? Which actors play the most historical figures?
- **Acceptance criteria**:
  - [x] Creator detail page shows which eras they gravitate toward (FIC-75) — TemporalObsessionMap with era-colored bars, setting year range, sample works
  - [x] Creator detail page shows which historical figures they've worked with most (FIC-76) — CastRepertoryCompany was already implemented
  - [x] Creator detail page shows their sentiment patterns across portrayals (FIC-77) — SentimentSignature with stacked color bar, per-sentiment detail, dominant tone summary
  - [x] Data-driven — only shows sections where there's enough data (min 3 works/portrayals) — analytics section hidden for creators with <3 works
- [x] Post-build review passed (CRITICAL: 0, HIGH: 0, MEDIUM: 0)
- **Linear tickets**: FIC-75, FIC-76, FIC-77
- **Key files**: `web-app/app/creator/[id]/page.tsx`, `web-app/lib/db.ts`

### M5: Production Polish + Cleanup — Complete
- **Why it matters**: Tighten the ship before broader beta invites.
- **Acceptance criteria**:
  - [x] Console.log statements removed from production code — removed from AddAppearanceForm, cache, auth, wikidata matching
  - [x] Admin-only routes gated behind role check — admin layout.tsx with auth redirect
  - [x] Duplicate code patterns consolidated (FIC-154) — cache, auth, health endpoint all use shared Neo4j driver
  - [x] FIC-149, FIC-148 tickets closed (work already done) — closed via Linear API
- [x] Post-build review passed (CRITICAL: 0, HIGH: 0, MEDIUM: 0)
- **Linear tickets**: FIC-154, FIC-149, FIC-148
- **Key files**: `web-app/app/admin/`, `web-app/app/api/`, `web-app/components/`

## Deferred (explicitly not this month)
- **Location data + map (FIC-132/133)** — enriches the product but lower impact than portrayal timeline and creator analytics
- **Side-by-side portrayal comparison (FIC-104)** — interesting but not differentiated
- **User auth + tracking (FIC-103)** — premature until there's an active beta audience
- **Multi-user contributions (FIC-45/46/47)** — need users first
- **Testing suites (FIC-54/55)** — should happen incrementally per milestone, not as standalone
- **Email digest (FIC-114)** — no subscriber base yet
- **AI Portrayal Researcher (FIC-111)** — overlaps with discovery agent, defer until that matures

## Dependencies
- M2 depends on George's decision to upgrade Gemini plan or switch providers
- M3 and M4 are independent of each other — can be parallelized
- M5 is independent — can run anytime

## Risks
- **Gemini upgrade cost** — monitor usage after upgrading. Batch illustration generation overnight if needed.
- **Medieval Europe cluster size (50+ figures)** — extraction pipeline hasn't run end-to-end yet. Budget extra time for entity resolution edge cases.
- **Creator analytics data quality** — need enough CREATED_BY relationships with actual creator names to be meaningful.
