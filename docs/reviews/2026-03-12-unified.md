# Code Review: staging vs main — 2026-03-12

**Reviewer**: Claude Code (triple review)
**Scope**: `git diff main...staging` — 2 commits, ~60 lines
**Commits reviewed**: 6aac6b2, 3fd8dc5

## Findings

| # | Severity | File | Finding | Action |
|---|----------|------|---------|--------|
| 1 | CRITICAL | `web-app/app/api/test-db/route.ts` | Unauthenticated endpoint exposes DB node count, sample node properties, and search results to any caller. No auth guard. | **Deleted** — debug route has no place in production. |
| 2 | HIGH | `web-app/app/api/test-db/route.ts` | Returns `error.stack` in JSON error responses — leaks internal file paths and call stacks to external users. | **Deleted** (same file). |
| 3 | HIGH | `web-app/app/api/test-db/route.ts` | Route contradicts documented security hardening. CLAUDE.md "Recent Completions > Security hardening" explicitly states "deleted test-db route". Re-adding it reverses prior security work. | **Deleted** (same file). |
| 4 | MEDIUM | `web-app/app/api/test-db/route.ts` | `console.error` in production code. CLAUDE.md states console.logs were removed in M5 Production Polish. | **Deleted** (same file). |
| 5 | LOW | `.gitignore` | Two extraneous blank lines appended at end of file. | **Removed** — trailing blank lines cleaned up. |

## Summary

The staging branch contained a re-introduced debug/test endpoint (`/api/test-db`) that had been explicitly deleted during prior security hardening work. The route was unauthenticated, leaked database contents and stack traces, and contradicted the project's documented security posture. Both issues have been resolved by deleting the route and cleaning up `.gitignore`.

No other changes were present in the staging diff.
